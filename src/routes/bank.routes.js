const express = require("express");
const router = express.Router();

// Placeholder for bank routes - to be implemented
router.get("/", (req, res) => {
  res.json({ message: "Bank routes - placeholder" });
});

module.exports = router;
