const express = require("express");
const router = express.Router();

// Placeholder for audit routes - to be implemented
router.get("/", (req, res) => {
  res.json({ message: "Audit routes - placeholder" });
});

module.exports = router;
